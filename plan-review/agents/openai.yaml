interface:
  display_name: "Plan Review"
  short_description: "Review plans and apply accepted findings"
  default_prompt: "Use $plan-review to review this code-related plan with a fresh sub-agent, apply accepted findings to the plan, and report accepted and rejected findings."
